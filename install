###############################
# install.sh                  #
# github.com/manuGMG/dotfiles #
###############################

# Check for root privileges ###
[ $EUID != 0 ] && echo "Run as root." && exit 1

# Install dependencies ########
sudo xbps-install -Su
sudo xbps-install -Su -y gcc make libX11-devel libXft-devel libXinerama-devel

# Install packages     ########
sudo xbps-install -Su -y alacritty nvim patch

# Compile dwm and dmenu #######
sudo make install -C dwm/
sudo make install -C dmenu/

# Copy .user (scripts) folder #
mkdir ~/.user
cp -r dmenu/ ~/.user/
cp -r dwm/ ~/.user/
cp bar ~/.user/
cp bar_dmenu ~/.user/

# Copy configuration files ####
cp .xinitrc ~/
cp .bashrc ~/
cp .bash_profile ~/
cp .alacritty.yml ~/
cp -r .config/ ~/

# Copy font files #############
cp -r fonts/ ~/.fonts/

# Output ######################
echo "* => Remember to run :PlugInstall for nvim!"
